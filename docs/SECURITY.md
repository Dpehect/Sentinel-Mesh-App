# Security model

Repository URLs are restricted to public GitHub HTTPS URLs in the worker example. Production deployments should run scanner jobs in network-restricted disposable containers with CPU, memory and timeout limits.
