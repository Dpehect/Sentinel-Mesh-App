# Optional scanners

Install Semgrep, Gitleaks and OSV Scanner through their official installers. The worker detects each executable with `which`/`where`; missing scanners are skipped without breaking the built-in scanner.
