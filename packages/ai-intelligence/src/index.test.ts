import {describe,expect,it} from "vitest";import {analyzeSecurityIntelligence,clusterFindings} from "./index";import type {Finding,ScanResult} from "@sentinel/security-core";
const base=(id:string,title:string):Finding=>({id,ruleId:"secret.generic",title,description:"hardcoded credential in source",severity:"high",confidence:.9,category:"secret",scanner:"builtin",cwe:"CWE-798",evidence:[{file:"a.ts",line:2,excerpt:"apiKey='secret-value'",source:"source"}],fingerprint:id});
describe("security intelligence",()=>{it("clusters semantically related findings",()=>{expect(clusterFindings([base("1","Hardcoded API key"),base("2","Embedded application secret")])[0].findingIds.length).toBe(2)});it("produces review intelligence",()=>{const s:ScanResult={repository:"x",startedAt:"x",completedAt:"x",findings:[base("1","Hardcoded API key")],assets:[],relations:[],attackPaths:[],score:70};expect(analyzeSecurityIntelligence(s).findings[0].priorityScore).toBeGreaterThan(40)})});
