"use client";import {useState} from "react";import {useRouter} from "next/navigation";export function ProjectForm(){const router=useRouter();const [name,setName]=useState("");const [repositoryUrl,setRepositoryUrl]=useState("");const [message,setMessage]=useState("");async function submit(){setMessage("Creating project…");const r=await fetch("/api/projects",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({name,repositoryUrl,defaultBranch:"main"})});const body=await r.json();if(!r.ok){setMessage(typeof body.error==="string"?body.error:"Project could not be created");return}setName("");setRepositoryUrl("");setMessage("Project created");router.refresh()}return <section className="card"><h2 className="section-title">Connect repository</h2><div className="two-field"><input className="input" placeholder="Project name" value={name} onChange={e=>setName(e.target.value)}/><input className="input" placeholder="https://github.com/owner/repository" value={repositoryUrl} onChange={e=>setRepositoryUrl(e.target.value)}/></div><button className="button" type="button" onClick={submit} disabled={!name||!repositoryUrl} style={{marginTop:10}}>Create project</button>{message&&<p className="muted" aria-live="polite">{message}</p>}</section>}