import {PublicNav} from "@/components/public-nav";import {demo} from "@/lib/demo";import {SecurityMesh} from "@/components/security-mesh";export default function Report(){return <><PublicNav/><main className="content" style={{paddingTop:60}}><div className="eyebrow">Public security report</div><h1 className="title">example/vulnerable-commerce</h1><p className="subtitle">Reproducible security posture report generated by Sentinel Mesh.</p><section className="metrics grid">{[["Score",demo.score],["Findings",demo.findings.length],["Attack paths",demo.attackPaths.length],["Assets",demo.assets.length]].map(([l,v])=><div className="card" key={l}><div className="metric-label">{l}</div><div className="metric-value">{v}</div></div>)}</section><div className="two grid"><div className="card"><h2 className="section-title">System topology</h2><SecurityMesh assets={demo.assets} relations={demo.relations}/></div><div className="card"><h2 className="section-title">Methodology</h2><p className="muted">Static source analysis, secret detection, dependency intelligence, asset discovery and graph traversal. This is a defensive report and does not exploit the target.</p><h2 className="section-title" style={{marginTop:24}}>Top findings</h2>{demo.findings.map(f=><div className="list-item" key={f.id}><span className={f.severity}>{f.severity}</span> · {f.title}</div>)}</div></div></main></>}
