import Link from "next/link";import {PublicNav} from "@/components/public-nav";import {LandingMesh} from "@/components/landing-mesh";export default function Home(){return <><PublicNav/><main className="hero"><section><div className="eyebrow">Open-source · local-first · defensive security</div><h1>Turn source code into an attack map.</h1><p className="subtitle">Sentinel Mesh unifies static analysis, secret scanning, dependency intelligence and asset discovery into explainable attack paths—without requiring a paid AI API.</p><div style={{display:"flex",gap:12,marginTop:28}}><Link className="button" href="/dashboard">Launch demo</Link><Link className="button" href="/docs">Read architecture</Link></div><div className="three grid" style={{marginTop:50}}>{[["4","Scanner engines"],["100%","Local-first"],["0","Paid API keys"]].map(([v,l])=><div key={l}><div className="metric-value">{v}</div><div className="metric-label">{l}</div></div>)}</div></section><LandingMesh/></main></>}
