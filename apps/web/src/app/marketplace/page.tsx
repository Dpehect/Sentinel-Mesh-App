import type {Metadata} from "next";
import {scannerCatalog} from "@/lib/catalog";
export const metadata:Metadata={title:"Scanner marketplace | Sentinel Mesh",description:"Bundled and optional security scanners supported by Sentinel Mesh."};
export default function Marketplace(){return <main className="public-page"><div className="eyebrow">Extensible security platform</div><h1 className="title">Scanner marketplace</h1><p className="subtitle">Use the built-in AST engine, attach free local scanners, or implement the typed plugin contract.</p><section className="market-grid">{scannerCatalog.map(item=><article className="card" key={item.id}><div className="row-between"><span className="badge">{item.category}</span><span className="muted">{item.status}</span></div><h2 className="section-title">{item.name}</h2><p className="muted">{item.description}</p><div className="chip-row">{item.capabilities.map(x=><span className="chip" key={x}>{x}</span>)}</div>{item.command?<code className="code-line">{item.command}</code>:null}</article>)}</section></main>}
