import { login } from "./actions";
export default async function Login({searchParams}:{searchParams:Promise<{error?:string}>}){const p=await searchParams;return <main className="auth-page"><section className="auth-card"><div className="eyebrow">Secure console</div><h1 className="title">Sign in to Sentinel Mesh</h1><p className="subtitle">Local-first credentials. No external identity provider is required in demo mode.</p>{p.error&&<div className="error-banner">Invalid email or password.</div>}<form action={login} className="form-grid"><label>Email<input name="email" type="email" defaultValue="admin@sentinel.local" required/></label><label>Password<input name="password" type="password" defaultValue="Sentinel123!" required/></label><button className="primary" type="submit">Open security console</button></form></section></main>}
