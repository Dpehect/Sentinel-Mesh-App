import {AttackPathView} from "@/components/attack-path-view";import {getLatestResult} from "@/lib/data";export const dynamic="force-dynamic";export default async function AttackPaths(){const result=await getLatestResult();return <><div className="eyebrow">Reachability intelligence</div><h1 className="title">Attack paths</h1><p className="subtitle">Explainable routes from public entry points to high-value assets in the latest persisted scan.</p><div style={{marginTop:24}}><AttackPathView result={result}/></>}