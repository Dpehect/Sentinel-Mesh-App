import {ScanLauncher} from "@/components/scan-launcher";import {getScans} from "@/lib/data";export const dynamic="force-dynamic";export default async function Scans(){const scans=await getScans();return <><div className="eyebrow">Scanner orchestration</div><h1 className="title">Scans</h1><p className="subtitle">Completed results persist to PostgreSQL when production mode is enabled.</p><div style={{marginTop:24}}><ScanLauncher/></div><div className="card table-wrap" style={{marginTop:16}}><h2 className="section-title">Recent jobs</h2><table className="table"><thead><tr><th>Repository</th><th>Status</th><th>Branch</th><th>Findings</th><th>Score</th></tr></thead><tbody>{scans.map(s=><tr key={s.id}><td>{s.repositoryUrl.replace("https://github.com/","")}</td><td><span className="badge">{s.status}</span></td><td>{s.branch??"main"}</td><td>{s.findingCount}</td><td>{s.score??"—"}</td></tr>)}</tbody></table></div></>}