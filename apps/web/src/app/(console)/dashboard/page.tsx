import {demo} from "@/lib/demo";import {SecurityMesh} from "@/components/security-mesh";import {RiskChart} from "@/components/risk-chart";export default function Dashboard(){const critical=demo.findings.filter(x=>x.severity==="critical").length;return <><div className="eyebrow">Command center</div><h1 className="title">Security posture</h1><p className="subtitle">Live view of repository risk, scanner coverage and reachable attack paths.</p><section className="metrics grid">{[["Security score",demo.score],["Critical findings",critical],["Attack paths",demo.attackPaths.length],["Assets mapped",demo.assets.length]].map(([l,v])=><div className="card" key={l}><div className="metric-label">{l}</div><div className="metric-value">{v}</div></div>)}</section><div className="two grid"><section className="card"><h2 className="section-title">Live security mesh</h2><SecurityMesh assets={demo.assets} relations={demo.relations}/></section><section className="grid"><div className="card"><h2 className="section-title">Risk timeline</h2><RiskChart/></div><div className="card"><h2 className="section-title">Priority queue</h2><div className="list">{demo.findings.map(f=><div className="list-item" key={f.id}><span className={f.severity}>{f.severity.toUpperCase()}</span><div style={{marginTop:6}}>{f.title}</div><div className="muted" style={{fontSize:12,marginTop:5}}>{f.evidence[0]?.file}</div></div>)}</div></div></section></div></>}
