import {enterpriseDemo} from "@/lib/enterprise-demo";import {complianceSummary} from "@sentinel/enterprise-core";
export default function EnterprisePage(){const summary=complianceSummary(enterpriseDemo.controls);return <><div className="eyebrow">Enterprise control plane</div><h1 className="title">Organizations, policy and compliance</h1><p className="subtitle">Manage teams, scoped API access, governance policies and framework evidence without external paid services.</p><section className="metrics grid">{[["Members",enterpriseDemo.organization.members],["Teams",enterpriseDemo.organization.teams],["Compliance",`${summary.coverage}%`],["Failing controls",summary.failing]].map(([l,v])=><div className="card" key={String(l)}><div className="metric-label">{l}</div><div className="metric-value">{v}</div></div>)}</section><div className="two grid"><section className="card"><h2 className="section-title">Teams</h2><div className="list">{enterpriseDemo.teams.map(t=><div className="list-item" key={t.name}><strong>{t.name}</strong><div className="muted">{t.members} members · {t.projects} projects</div></div>)}</div></section><section className="card"><h2 className="section-title">API tokens</h2><div className="list">{enterpriseDemo.tokens.map(t=><div className="list-item" key={t.name}><strong>{t.name}</strong><div className="muted">{t.prefix}_••••{t.lastFour} · {t.scopes.join(", ")}</div></div>)}</div></section></div><section className="card" style={{marginTop:18}}><h2 className="section-title">Compliance evidence</h2><div className="list">{enterpriseDemo.controls.map(c=><div className="list-item" key={`${c.framework}-${c.control}`}><span className={c.status==="passing"?"low":"high"}>{c.status.toUpperCase()}</span><div style={{marginTop:6}}><strong>{c.framework} {c.control}</strong> — {c.title}</div><div className="muted">{c.evidence.join(" · ")}</div></div>)}</div></section></>}
