import {getLatestResult} from "@/lib/data";export const dynamic="force-dynamic";export default async function Remediation(){const result=await getLatestResult();return <><div className="eyebrow">Root-cause workflow</div><h1 className="title">Remediation center</h1><p className="subtitle">Prioritized engineering work derived from the latest persisted findings.</p><div className="grid" style={{marginTop:24}}>{result.findings.map((f,i)=><article className="card" key={f.id}><div style={{display:"flex",justifyContent:"space-between",gap:12}}><div><span className={f.severity}>{f.severity.toUpperCase()}</span><h2 className="section-title" style={{marginTop:8}}>{f.title}</h2></div><span className="badge">Priority {i+1}</span></div><p className="muted">{f.remediation}</p><div className="code">1. Confirm the affected trust boundary.\n2. Apply the smallest secure code change.\n3. Add a regression test.\n4. Re-run scanners and compare the attack graph.</div></article>)}</div></>}