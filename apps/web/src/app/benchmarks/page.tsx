import type {Metadata} from "next";
import {benchmarkSamples} from "@/lib/catalog";
export const metadata:Metadata={title:"Security scanner benchmarks | Sentinel Mesh",description:"Reproducible local benchmark fixtures for Sentinel Mesh."};
export default function Benchmarks(){return <main className="public-page"><div className="eyebrow">Reproducible engineering</div><h1 className="title">Benchmark center</h1><p className="subtitle">Reference results are sample development measurements. Run the included benchmark script on your machine for authoritative numbers.</p><section className="card"><div className="table-wrap"><table><thead><tr><th>Fixture</th><th>Files</th><th>Duration</th><th>Memory</th><th>Findings</th></tr></thead><tbody>{benchmarkSamples.map(x=><tr key={x.fixture}><td>{x.fixture}</td><td>{x.files}</td><td>{(x.durationMs/1000).toFixed(2)} s</td><td>{x.memoryMb} MB</td><td>{x.findings}</td></tr>)}</tbody></table></div></section><section className="card"><h2 className="section-title">Run locally</h2><code className="code-line">npm run benchmark</code><p className="muted">The benchmark command scans local fixtures, records runtime and writes JSON output under benchmarks/results.</p></section></main>}
