import {PublicNav} from "@/components/public-nav";export default function Docs(){return <><PublicNav/><main className="content" style={{maxWidth:900,paddingTop:60}}><div className="eyebrow">Documentation</div><h1 className="title">Architecture and local setup</h1><div className="card"><h2>Execution flow</h2><div className="code">GitHub URL → isolated clone → scanner adapters → normalized findings → asset discovery → attack graph → remediation</div><h2>Zero-cost mode</h2><p className="muted">The built-in deterministic scanner and demo UI require no paid AI API. Optional command-line scanners run locally. Ollama is optional.</p><h2>Safety boundary</h2><p className="muted">Sentinel Mesh performs defensive repository analysis only. It does not send exploit payloads or scan remote hosts.</p></div></main></>}
